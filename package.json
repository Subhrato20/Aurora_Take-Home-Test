{
  "name": "aurora-take-home-test",
  "version": "1.0.0",
  "description": "Root package.json to help nixpacks detect Node.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd src/frontend && npm ci && npm run build"
  }
}

